# Create a new EmbeddedXlsxPart containing `blob`

Create a new EmbeddedXlsxPart containing `blob`

## Usage

``` r
EmbeddedXlsxPart_new(blob, package)
```

## Arguments

- blob:

  Raw bytes (the xlsx file).

- package:

  The OPC package.

## Value

An EmbeddedXlsxPart.
