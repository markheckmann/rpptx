# Content type URIs (like MIME-types) that specify a part's format.

Content type URIs (like MIME-types) that specify a part's format.

## Usage

``` r
CT
```

## Format

An object of class `list` of length 104.
