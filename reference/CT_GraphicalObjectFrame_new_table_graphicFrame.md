# Create a new \<p:graphicFrame\> wrapping a table

Create a new \<p:graphicFrame\> wrapping a table

## Usage

``` r
CT_GraphicalObjectFrame_new_table_graphicFrame(
  id,
  name,
  rows,
  cols,
  x,
  y,
  cx,
  cy
)
```
