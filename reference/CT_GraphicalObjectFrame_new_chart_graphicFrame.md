# Create a new \<p:graphicFrame\> referencing a chart part via rId

Create a new \<p:graphicFrame\> referencing a chart part via rId

## Usage

``` r
CT_GraphicalObjectFrame_new_chart_graphicFrame(id, name, rId, x, y, cx, cy)
```
