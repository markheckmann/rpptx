# Define a RequiredAttribute specification

Define a RequiredAttribute specification

## Usage

``` r
required_attribute(attr_name, simple_type)
```

## Arguments

- attr_name:

  Attribute name (may be namespace-prefixed).

- simple_type:

  A list with `from_xml` and `to_xml` functions.

## Value

A list describing the attribute spec.
