# Connector type constants (MSO_CONNECTOR_TYPE)

Members have `$value` (integer) and `$prst` (XML preset name). Alias:
`MSO_CONNECTOR`.

## Usage

``` r
MSO_CONNECTOR_TYPE

MSO_CONNECTOR
```

## Format

An object of class `list` of length 4.

An object of class `list` of length 4.
