# Serialize a BaseOxmlElement to XML bytes

Serialize a BaseOxmlElement to XML bytes

## Usage

``` r
serialize_part_xml(element)
```

## Arguments

- element:

  A BaseOxmlElement.

## Value

A raw vector of XML bytes.
