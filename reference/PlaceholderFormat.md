# Placeholder format descriptor

Provides access to placeholder properties such as `idx` and `type`.
Accessed via `shape$placeholder_format` on a placeholder shape.

## Methods

### Public methods

- [`PlaceholderFormat$new()`](#method-PlaceholderFormat-new)

- [`PlaceholderFormat$element()`](#method-PlaceholderFormat-element)

- [`PlaceholderFormat$clone()`](#method-PlaceholderFormat-clone)

------------------------------------------------------------------------

### Method `new()`

#### Usage

    PlaceholderFormat$new(ph_elm)

------------------------------------------------------------------------

### Method `element()`

#### Usage

    PlaceholderFormat$element()

------------------------------------------------------------------------

### Method `clone()`

The objects of this class are cloneable with this method.

#### Usage

    PlaceholderFormat$clone(deep = FALSE)

#### Arguments

- `deep`:

  Whether to make a deep clone.
