# Get a relative reference from a PackURI to a base URI

Get a relative reference from a PackURI to a base URI

## Usage

``` r
pack_uri_relative_ref(uri, base_uri)
```

## Arguments

- uri:

  A PackURI.

- base_uri:

  The base URI to compute the relative reference from.

## Value

A character string.
