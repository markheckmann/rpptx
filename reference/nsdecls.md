# Return namespace declaration strings

Return namespace declaration strings

## Usage

``` r
nsdecls(...)
```

## Arguments

- ...:

  Namespace prefixes as character strings.

## Value

A single string with xmlns declarations, e.g. `'xmlns:p="http://..."'`.
