# Load and parse an XML template file

Load and parse an XML template file

## Usage

``` r
parse_from_template(template_name)
```

## Arguments

- template_name:

  Name of the template (without .xml extension).

## Value

A wrapped element for the template root.
