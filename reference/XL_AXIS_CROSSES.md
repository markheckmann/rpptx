# XL_AXIS_CROSSES — where the other axis crosses this axis

XML string values for `c:crosses/@val`. Use with axis `crosses`
property.

## Usage

``` r
XL_AXIS_CROSSES
```

## Format

An object of class `list` of length 4.
