# Write items to a ZIP file

Write items to a ZIP file

## Usage

``` r
.write_zip_package(pkg_file, items)
```

## Arguments

- pkg_file:

  Path to output file.

- items:

  A list of lists with `membername` and `blob` elements.
