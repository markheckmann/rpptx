# PP_PARAGRAPH_ALIGNMENT — paragraph alignment

XML string values for `a:pPr/@algn`. Use with paragraph `alignment`
property.

## Usage

``` r
PP_PARAGRAPH_ALIGNMENT
```

## Format

An object of class `list` of length 8.
