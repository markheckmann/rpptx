# Get the namespace URI for a given prefix

Get the namespace URI for a given prefix

## Usage

``` r
nsuri(nspfx)
```

## Arguments

- nspfx:

  A namespace prefix string, e.g. `"p"`.

## Value

The namespace URI string.
