# Create a simple type converter

Create a simple type converter

## Usage

``` r
simple_type(from_xml, to_xml)
```

## Arguments

- from_xml:

  Function(str) -\> R value.

- to_xml:

  Function(R value) -\> str.

## Value

A list with `from_xml` and `to_xml` elements.
